spring:
  application:
    name: dondoc-backend

  profiles:
    active: local

  datasource:
    url: jdbc:mysql://localhost:3306/dondoc
    username: root
    password: asdf
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

app:
  default-farm-id: 1